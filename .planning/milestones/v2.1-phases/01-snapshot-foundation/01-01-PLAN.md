---
phase: 01-snapshot-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RoslynNavigator/RoslynNavigator.csproj
  - src/RoslynNavigator/Resources/SnapshotSchema.sql
  - src/RoslynNavigator/Services/SnapshotSchemaService.cs
  - src/RoslynNavigator/Services/SnapshotPathService.cs
  - tests/RoslynNavigator.Tests/SnapshotSchemaServiceTests.cs
autonomous: true
requirements: [SNAP-02, SNAP-04]

must_haves:
  truths:
    - "Snapshot schema is created idempotently with all required tables before any data write"
    - "Schema is loaded from embedded `SnapshotSchema.sql`, not hardcoded SQL strings"
    - "Default snapshot path resolves consistently from solution path when `--db` is omitted"
  artifacts:
    - path: "src/RoslynNavigator/Resources/SnapshotSchema.sql"
      provides: "Canonical SQLite schema for classes, methods, dependencies, calls, annotations, flags, snapshot_meta"
      contains: "CREATE TABLE IF NOT EXISTS classes"
    - path: "src/RoslynNavigator/Services/SnapshotSchemaService.cs"
      provides: "Embedded schema loading and idempotent schema application"
      exports: ["SnapshotSchemaService"]
    - path: "src/RoslynNavigator/Services/SnapshotPathService.cs"
      provides: "Default DB path resolution and parent directory creation"
      exports: ["SnapshotPathService"]
  key_links:
    - from: "src/RoslynNavigator/Services/SnapshotSchemaService.cs"
      to: "src/RoslynNavigator/Resources/SnapshotSchema.sql"
      via: "Assembly.GetManifestResourceStream"
      pattern: "SnapshotSchema.sql"
    - from: "src/RoslynNavigator/RoslynNavigator.csproj"
      to: "src/RoslynNavigator/Resources/SnapshotSchema.sql"
      via: "EmbeddedResource item"
      pattern: "EmbeddedResource"
---

<objective>
Create the snapshot storage foundation: SQLite dependency, embedded schema, schema bootstrap service, and deterministic default DB path behavior.

Purpose: Phase 1 must start from a stable and repeatable DB bootstrap so later extraction logic can focus on data, not setup drift.
Output: SnapshotSchema.sql embedded resource, schema/path services, and tests validating idempotent table creation.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/RoslynNavigator/RoslynNavigator.csproj
@src/RoslynNavigator/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite package and embedded schema resource</name>
  <files>
src/RoslynNavigator/RoslynNavigator.csproj
src/RoslynNavigator/Resources/SnapshotSchema.sql
  </files>
  <action>
Add `Microsoft.Data.Sqlite` to `RoslynNavigator.csproj` and register `Resources/SnapshotSchema.sql` as an `EmbeddedResource`.

Create `SnapshotSchema.sql` with idempotent DDL for exactly these tables: `classes`, `methods`, `dependencies`, `calls`, `annotations`, `flags`, `snapshot_meta`.

Include required signal columns (`returns_null`, `cognitive_complexity`, `has_try_catch`, `calls_external`, `accesses_db`, `filters_by_tenant`) in method/class storage tables so Phase 1 can persist analysis signals without schema revisions.

Use explicit primary keys and foreign keys where applicable; avoid runtime-generated SQL strings in C#.
  </action>
  <verify>
Run `dotnet build RoslynNavigator.sln` and confirm no compile errors after package/resource additions.
  </verify>
  <done>
SQLite package and embedded schema are present; schema file defines all required tables and signal columns with idempotent `CREATE TABLE IF NOT EXISTS` statements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement schema bootstrap and default path services</name>
  <files>
src/RoslynNavigator/Services/SnapshotSchemaService.cs
src/RoslynNavigator/Services/SnapshotPathService.cs
  </files>
  <action>
Create `SnapshotSchemaService` that opens/creates the SQLite file, loads embedded SQL from `SnapshotSchema.sql`, executes it transactionally, and writes/updates a `snapshot_meta` row with generation timestamp and solution path.

Create `SnapshotPathService` that resolves default DB path from `--solution` (e.g., sibling `.roslyn-nav/snapshots/<solution-name>.snapshot.db`), normalizes absolute paths, and creates parent directories.

Keep services deterministic and side-effect scoped; do not wire CLI command yet (that is plan 01-03).
  </action>
  <verify>
Run `dotnet test tests/RoslynNavigator.Tests/RoslynNavigator.Tests.csproj --filter SnapshotSchemaServiceTests` after adding tests in Task 3.
  </verify>
  <done>
Services compile and can initialize a DB with full schema from embedded SQL while resolving default output path consistently.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add schema bootstrap tests</name>
  <files>tests/RoslynNavigator.Tests/SnapshotSchemaServiceTests.cs</files>
  <action>
Add tests that create temporary DB files and verify:
1) all required tables exist,
2) schema initialization is idempotent (second run does not fail or duplicate),
3) `snapshot_meta` is written/updated,
4) default path logic produces deterministic location per solution path.

Reuse existing temp-directory cleanup style from current test suite.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter SnapshotSchemaServiceTests`.
  </verify>
  <done>
Tests pass and prove schema/path foundation behavior needed by SNAP-02 and SNAP-04.
  </done>
</task>

</tasks>

<verification>
Run `dotnet test RoslynNavigator.sln --filter SnapshotSchemaServiceTests` and `dotnet build RoslynNavigator.sln`.
Confirm embedded resource entry exists in `src/RoslynNavigator/RoslynNavigator.csproj` and `SnapshotSchema.sql` is used by service loading logic.
</verification>

<success_criteria>
- DB bootstrap creates all required tables with idempotent schema application
- Schema is sourced from embedded `SnapshotSchema.sql`
- Default path resolution works without `--db`
- Foundation tests pass in CI/local execution
</success_criteria>

<output>
After completion, create `.planning/phases/01-snapshot-foundation/01-01-SUMMARY.md`
</output>
