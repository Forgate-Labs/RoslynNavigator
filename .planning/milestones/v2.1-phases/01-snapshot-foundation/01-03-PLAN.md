---
phase: 01-snapshot-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/RoslynNavigator/Models/SnapshotCommandResults.cs
  - src/RoslynNavigator/Commands/SnapshotCommand.cs
  - src/RoslynNavigator/Program.cs
  - tests/RoslynNavigator.Tests/SnapshotCommandTests.cs
autonomous: true
requirements: [SNAP-01, SNAP-04]

must_haves:
  truths:
    - "User can run `roslyn-nav snapshot --solution <path.sln>` and receive successful JSON output"
    - "When `--db` is omitted, command creates snapshot at the default path"
    - "Command output reports where DB was created and summary counts for persisted entities"
  artifacts:
    - path: "src/RoslynNavigator/Commands/SnapshotCommand.cs"
      provides: "CLI command handler that orchestrates path, schema bootstrap, and extraction"
      exports: ["SnapshotCommand"]
    - path: "src/RoslynNavigator/Models/SnapshotCommandResults.cs"
      provides: "Machine-readable snapshot command output contract"
      exports: ["SnapshotCommandResult"]
    - path: "src/RoslynNavigator/Program.cs"
      provides: "`snapshot` command registration on root CLI"
      contains: "new Command(\"snapshot\""
  key_links:
    - from: "src/RoslynNavigator/Program.cs"
      to: "src/RoslynNavigator/Commands/SnapshotCommand.cs"
      via: "SetHandler invoking SnapshotCommand.ExecuteAsync"
      pattern: "SnapshotCommand.ExecuteAsync"
    - from: "src/RoslynNavigator/Commands/SnapshotCommand.cs"
      to: "src/RoslynNavigator/Services/SnapshotSchemaService.cs"
      via: "schema initialization before extraction"
      pattern: "SnapshotSchemaService"
---

<objective>
Expose Snapshot Foundation through the CLI so users can generate a real snapshot DB in one command.

Purpose: Phase goal is user-observable command behavior; this plan turns services into the final `roslyn-nav snapshot` entrypoint.
Output: Snapshot command wiring, JSON result model, and command-level tests.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/RoslynNavigator/Program.cs
@src/RoslynNavigator/Models/ErrorResult.cs
@src/RoslynNavigator/Commands/FileReadCommand.cs
@src/RoslynNavigator/Services/SnapshotSchemaService.cs
@src/RoslynNavigator/Services/SnapshotExtractorService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement snapshot command contract and orchestrator</name>
  <files>
src/RoslynNavigator/Models/SnapshotCommandResults.cs
src/RoslynNavigator/Commands/SnapshotCommand.cs
  </files>
  <action>
Create `SnapshotCommandResult` with fields needed for machine output: solution path, db path, table/entity counts, elapsed time, and status metadata.

Implement `SnapshotCommand.ExecuteAsync(string solutionPath, string? dbPath)` that:
1) resolves output path using `SnapshotPathService`,
2) initializes schema using `SnapshotSchemaService`,
3) runs extraction via `SnapshotExtractorService`,
4) returns structured result.

Ensure exceptions flow to Program.cs error wrapper style (`OutputError`), not ad-hoc console output in command class.
  </action>
  <verify>
Run `dotnet build RoslynNavigator.sln`.
  </verify>
  <done>
Snapshot command class returns structured JSON-ready result and orchestrates full snapshot lifecycle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `snapshot` command in Program.cs</name>
  <files>src/RoslynNavigator/Program.cs</files>
  <action>
Add root-level `snapshot` command with required `--solution` and optional `--db` arguments.

Follow existing command style:
- define command and options near other root commands,
- `SetHandler` with try/catch,
- serialize success result with existing `jsonOptions`,
- report failures via `OutputError("snapshot_error", ex.Message)`.

Register command in root command list so `roslyn-nav --help` surfaces it.
  </action>
  <verify>
Run `dotnet run --project src/RoslynNavigator -- --help` and confirm `snapshot` appears.
  </verify>
  <done>
`snapshot` is discoverable in CLI help and invokes SnapshotCommand with expected arguments.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add command-level tests for default and explicit DB path</name>
  <files>tests/RoslynNavigator.Tests/SnapshotCommandTests.cs</files>
  <action>
Add tests validating:
1) `ExecuteAsync` succeeds with explicit `--db` path and DB file exists,
2) `ExecuteAsync` without `--db` uses default path convention,
3) result payload includes db path and non-negative entity counts,
4) command run over sample solution creates a DB containing required tables.

Use temp directories and deterministic cleanup pattern used elsewhere in test project.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter SnapshotCommandTests`.
  </verify>
  <done>
Command behavior required by SNAP-01 is proven in automated tests for both explicit and default path flows.
  </done>
</task>

</tasks>

<verification>
Run:
- `dotnet test RoslynNavigator.sln --filter Snapshot`
- `dotnet run --project src/RoslynNavigator -- snapshot --solution tests/SampleSolution/Sample.sln`

Confirm command emits JSON, DB file exists, and schema tables are present.
</verification>

<success_criteria>
- `roslyn-nav snapshot --solution <path.sln>` produces snapshot DB and JSON response
- Default DB path works when `--db` is omitted
- `snapshot` appears in `--help` output
- Snapshot command tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-snapshot-foundation/01-03-SUMMARY.md`
</output>
