---
phase: 02-rules-engine
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/RoslynNavigator/Commands/CheckCommand.cs
  - src/RoslynNavigator/Models/CheckCommandResults.cs
  - src/RoslynNavigator/Program.cs
  - tests/RoslynNavigator.Tests/CheckCommandTests.cs
autonomous: true
requirements: [RULE-01, RULE-05]

must_haves:
  truths:
    - "User can run `roslyn-nav check` and receive JSON violations from snapshot data"
    - "Output can be filtered by severity and by ruleId without changing rule evaluation correctness"
    - "CLI errors still follow the existing `ErrorResult` JSON contract"
  artifacts:
    - path: "src/RoslynNavigator/Commands/CheckCommand.cs"
      provides: "Check command orchestrator (load rules, evaluate, filter, return result)"
      exports: ["CheckCommand"]
    - path: "src/RoslynNavigator/Models/CheckCommandResults.cs"
      provides: "JSON response models for check execution and filtered violations"
      exports: ["CheckCommandResult"]
    - path: "src/RoslynNavigator/Program.cs"
      provides: "CLI registration for `check`, `--severity`, `--ruleId`, and `--db` options"
      contains: "new Command(\"check\""
  key_links:
    - from: "src/RoslynNavigator/Commands/CheckCommand.cs"
      to: "src/RoslynNavigator/Services/RuleLoaderService.cs"
      via: "Load all available rules before evaluation"
      pattern: "RuleLoaderService"
    - from: "src/RoslynNavigator/Commands/CheckCommand.cs"
      to: "src/RoslynNavigator/Services/RuleEvaluatorService.cs"
      via: "Evaluate loaded rules on target snapshot"
      pattern: "RuleEvaluatorService"
---

<objective>
Expose the rules engine through the CLI with production-ready filtering and output contracts.

Purpose: RULE-01 and RULE-05 are user-facing and complete the phase value only when command wiring and output filtering are integrated.
Output: `check` command, result models, CLI wiring, and end-to-end command tests.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rules-engine/02-01-SUMMARY.md
@.planning/phases/02-rules-engine/02-02-SUMMARY.md
@src/RoslynNavigator/Program.cs
@src/RoslynNavigator/Commands/SnapshotCommand.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement check command orchestration and result models</name>
  <files>
src/RoslynNavigator/Commands/CheckCommand.cs
src/RoslynNavigator/Models/CheckCommandResults.cs
  </files>
  <action>
Create `CheckCommand` that accepts snapshot db path and optional filters (`severity`, `ruleId`), loads rules via `RuleLoaderService`, evaluates via `RuleEvaluatorService`, then applies filters in-memory before returning result payload.

Create `CheckCommandResult` model with fields for db path, total rules evaluated, violation count, filtered count, and violations list.

Reuse existing command pattern from `SnapshotCommand` for predictable error handling and testability.
  </action>
  <verify>
Run `dotnet build RoslynNavigator.sln` after wiring models/command.
  </verify>
  <done>
Command orchestration compiles and returns structured JSON-ready result data for full and filtered runs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `check` command and filter options in Program.cs</name>
  <files>src/RoslynNavigator/Program.cs</files>
  <action>
Register a top-level `check` command in `Program.cs` with options:
- `--db` optional snapshot path (uses default snapshot path resolution when omitted),
- `--severity` optional filter,
- `--ruleId` optional filter.

Serialize success output with existing `jsonOptions`, and route exceptions to `OutputError("check_error", ...)` to preserve CLI contract consistency.

Ensure `check` appears in `roslyn-nav --help`.
  </action>
  <verify>
Run `dotnet run --project src/RoslynNavigator -- --help` and confirm `check` appears in command list.
  </verify>
  <done>
`roslyn-nav check` is discoverable and wired with required filtering options.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add command-level tests for check output and filtering</name>
  <files>tests/RoslynNavigator.Tests/CheckCommandTests.cs</files>
  <action>
Add tests that validate:
1) `check` returns JSON violation payload against a prepared snapshot,
2) `--severity` filter restricts results correctly,
3) `--ruleId` filter restricts results correctly,
4) combined filters produce intersection behavior,
5) invalid DB/rule input returns error payload path.

Keep fixtures deterministic by generating temp snapshot DB content in test setup.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter CheckCommandTests` then `dotnet test RoslynNavigator.sln`.
  </verify>
  <done>
Command tests pass and prove RULE-01 and RULE-05 user-visible behavior.
  </done>
</task>

</tasks>

<verification>
Run `dotnet test RoslynNavigator.sln --filter CheckCommandTests`.
Run `dotnet run --project src/RoslynNavigator -- check --solution tests/SampleSolution/Sample.sln` (with prepared snapshot) and validate JSON output shape.
</verification>

<success_criteria>
- `roslyn-nav check` returns JSON violations for existing snapshot data
- `--severity` and `--ruleId` filters are implemented and test-covered
- Command follows existing CLI JSON and error contract patterns
</success_criteria>

<output>
After completion, create `.planning/phases/02-rules-engine/02-03-SUMMARY.md`
</output>
