---
phase: 02-rules-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RoslynNavigator/RoslynNavigator.csproj
  - src/RoslynNavigator/Resources/Rules/architecture.yaml
  - src/RoslynNavigator/Resources/Rules/code-quality.yaml
  - src/RoslynNavigator/Resources/Rules/security.yaml
  - src/RoslynNavigator/Models/RuleModels.cs
  - src/RoslynNavigator/Services/RuleLoaderService.cs
  - tests/RoslynNavigator.Tests/RuleLoaderServiceTests.cs
autonomous: true
requirements: [RULE-02]

must_haves:
  truths:
    - "Rules engine can load builtin rule packs without any external files present"
    - "Optional domain rules under `roslyn-nav-rules/` are discovered and merged"
    - "Malformed YAML does not crash silently; loader returns actionable errors"
  artifacts:
    - path: "src/RoslynNavigator/Resources/Rules/architecture.yaml"
      provides: "Builtin architecture rules pack embedded in assembly"
      contains: "rules:"
    - path: "src/RoslynNavigator/Services/RuleLoaderService.cs"
      provides: "Unified loading of embedded + optional domain YAML rule files"
      exports: ["RuleLoaderService"]
    - path: "src/RoslynNavigator/Models/RuleModels.cs"
      provides: "Typed rule/predicate/severity models shared by loader and evaluator"
      exports: ["RuleDefinition", "RulePredicate"]
  key_links:
    - from: "src/RoslynNavigator/RoslynNavigator.csproj"
      to: "src/RoslynNavigator/Resources/Rules/architecture.yaml"
      via: "EmbeddedResource include"
      pattern: "Resources\\Rules\\"
    - from: "src/RoslynNavigator/Services/RuleLoaderService.cs"
      to: "src/RoslynNavigator/Models/RuleModels.cs"
      via: "YamlDotNet deserialization into typed models"
      pattern: "Deserializer"
---

<objective>
Establish rule-definition foundations by introducing typed rule models and a deterministic loader for builtin and domain YAML packs.

Purpose: RULE-02 requires stable rule ingestion before SQL compilation/evaluation can be implemented safely.
Output: Embedded builtin YAML packs, loader service, and tests for merge/error behavior.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rules-engine/02-DISCOVERY.md
@src/RoslynNavigator/RoslynNavigator.csproj
@src/RoslynNavigator/Services/SnapshotSchemaService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add YAML dependency and embed builtin rule packs</name>
  <files>
src/RoslynNavigator/RoslynNavigator.csproj
src/RoslynNavigator/Resources/Rules/architecture.yaml
src/RoslynNavigator/Resources/Rules/code-quality.yaml
src/RoslynNavigator/Resources/Rules/security.yaml
  </files>
  <action>
Add `YamlDotNet` package reference in `RoslynNavigator.csproj` and register `Resources/Rules/*.yaml` as embedded resources.

Create three builtin packs (`architecture.yaml`, `code-quality.yaml`, `security.yaml`) with realistic starter rules including `id`, `title`, `severity`, `message`, and predicate blocks used by phase requirements.

Do not hardcode rule content in C# strings; all builtin rules must live in YAML files and be loaded via resources.
  </action>
  <verify>
Run `dotnet build RoslynNavigator.sln` and confirm no package/resource build errors.
  </verify>
  <done>
Project builds with YamlDotNet installed and builtin rule files embedded in the assembly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement typed rule contracts and loader service</name>
  <files>
src/RoslynNavigator/Models/RuleModels.cs
src/RoslynNavigator/Services/RuleLoaderService.cs
  </files>
  <action>
Create rule model contracts for pack/root/rule/predicate structures with strict field naming that maps YAML keys exactly.

Implement `RuleLoaderService` that:
1) loads builtin packs from embedded resources,
2) scans optional `roslyn-nav-rules/` directory for domain YAML files,
3) merges both sources into one ordered in-memory list,
4) surfaces parse/validation failures with file context.

Keep loading side-effect free (no writes to snapshot DB) and deterministic across runs.
  </action>
  <verify>
Run `dotnet test tests/RoslynNavigator.Tests/RoslynNavigator.Tests.csproj --filter RuleLoaderServiceTests` after Task 3 is implemented.
  </verify>
  <done>
Loader returns unified rule definitions from builtin and optional domain files with actionable errors for invalid YAML.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add rule loader tests for builtin, domain, and invalid YAML cases</name>
  <files>tests/RoslynNavigator.Tests/RuleLoaderServiceTests.cs</files>
  <action>
Add tests that validate:
1) builtin packs are discovered from embedded resources,
2) custom files in `roslyn-nav-rules/` are merged,
3) duplicate rule IDs are handled deterministically,
4) malformed YAML returns structured failure details (file + reason).

Use temporary directories for domain-rule fixtures; do not depend on machine-specific paths.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter RuleLoaderServiceTests`.
  </verify>
  <done>
Tests pass and prove RULE-02 loader behavior for builtin and domain rule sources.
  </done>
</task>

</tasks>

<verification>
Run `dotnet test RoslynNavigator.sln --filter RuleLoaderServiceTests` and `dotnet build RoslynNavigator.sln`.
Confirm `Resources/Rules/*.yaml` are listed as embedded resources in `src/RoslynNavigator/RoslynNavigator.csproj`.
</verification>

<success_criteria>
- Builtin rule packs are loaded from embedded YAML resources
- Optional domain rules in `roslyn-nav-rules/` are merged into the same rule set
- Invalid YAML produces explicit, test-covered loader errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-rules-engine/02-01-SUMMARY.md`
</output>
