---
phase: 02-rules-engine
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - src/RoslynNavigator/Services/RuleSqlCompiler.cs
  - src/RoslynNavigator/Services/RuleEvaluatorService.cs
  - src/RoslynNavigator/Models/RuleViolationModels.cs
  - tests/RoslynNavigator.Tests/RuleSqlCompilerTests.cs
  - tests/RoslynNavigator.Tests/RuleEvaluatorServiceTests.cs
autonomous: true
requirements: [RULE-03, RULE-04]

must_haves:
  truths:
    - "Wildcard predicates like `IRepo.*` are evaluated through SQL LIKE semantics"
    - "`not:` blocks negate nested predicate existence via NOT EXISTS"
    - "Rule evaluation executes read-only SQL and returns deterministic violation rows"
  artifacts:
    - path: "src/RoslynNavigator/Services/RuleSqlCompiler.cs"
      provides: "Translation from rule predicates into parameterized SQL fragments"
      exports: ["RuleSqlCompiler"]
    - path: "src/RoslynNavigator/Services/RuleEvaluatorService.cs"
      provides: "Execution of compiled SQL against snapshot DB and violation collection"
      exports: ["RuleEvaluatorService"]
    - path: "tests/RoslynNavigator.Tests/RuleSqlCompilerTests.cs"
      provides: "Regression coverage for LIKE and NOT EXISTS SQL generation"
      contains: "NOT EXISTS"
  key_links:
    - from: "src/RoslynNavigator/Services/RuleEvaluatorService.cs"
      to: "src/RoslynNavigator/Services/RuleSqlCompiler.cs"
      via: "Compile before execution"
      pattern: "Compile"
    - from: "src/RoslynNavigator/Services/RuleEvaluatorService.cs"
      to: "src/RoslynNavigator/Resources/SnapshotSchema.sql"
      via: "SELECT-only queries over snapshot tables"
      pattern: "SELECT"
---

<objective>
Implement SQL compilation and evaluation semantics with TDD so wildcard and negation behavior is locked by tests before CLI wiring.

Purpose: RULE-03 and RULE-04 are correctness-sensitive; TDD prevents subtle query-regression bugs.
Output: SQL compiler, evaluator service, violation models, and green semantic tests.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rules-engine/02-01-SUMMARY.md
@src/RoslynNavigator/Resources/SnapshotSchema.sql
@src/RoslynNavigator/Models/RuleModels.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - add failing tests for wildcard and not semantics</name>
  <files>
tests/RoslynNavigator.Tests/RuleSqlCompilerTests.cs
tests/RoslynNavigator.Tests/RuleEvaluatorServiceTests.cs
  </files>
  <action>
Create failing tests that pin required behavior:
1) `calls: IRepo.*` compiles/evaluates using SQL `LIKE` with wildcard conversion,
2) `not:` block compiles/evaluates with `NOT EXISTS`,
3) nested predicate combinations preserve logical intent.

Include at least one integration-style evaluator test that runs against a temp snapshot DB seeded with minimal rows.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter "RuleSqlCompilerTests|RuleEvaluatorServiceTests"` and confirm new tests fail first.
  </verify>
  <done>
New tests exist and fail specifically on missing LIKE / NOT EXISTS semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement SQL compiler, evaluator, and violation models</name>
  <files>
src/RoslynNavigator/Services/RuleSqlCompiler.cs
src/RoslynNavigator/Services/RuleEvaluatorService.cs
src/RoslynNavigator/Models/RuleViolationModels.cs
tests/RoslynNavigator.Tests/RuleSqlCompilerTests.cs
tests/RoslynNavigator.Tests/RuleEvaluatorServiceTests.cs
  </files>
  <action>
Implement `RuleSqlCompiler` to translate predicates into parameterized SQL with:
- wildcard conversion (`*` -> `%`) for LIKE,
- `not:` translation to `NOT EXISTS (...)`,
- explicit table/column mapping aligned to snapshot schema.

Implement `RuleEvaluatorService` to execute compiled queries as read-only `SELECT` statements and return typed violation rows (`ruleId`, `severity`, `message`, entity context).

Keep evaluator free of CLI concerns and avoid any SQL write statements.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter "RuleSqlCompilerTests|RuleEvaluatorServiceTests"` until green.
  </verify>
  <done>
Compiler and evaluator satisfy all semantic tests for RULE-03 and RULE-04.
  </done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR - harden query safety and readability</name>
  <files>
src/RoslynNavigator/Services/RuleSqlCompiler.cs
src/RoslynNavigator/Services/RuleEvaluatorService.cs
tests/RoslynNavigator.Tests/RuleSqlCompilerTests.cs
tests/RoslynNavigator.Tests/RuleEvaluatorServiceTests.cs
  </files>
  <action>
Refactor compiler/evaluator internals for maintainability (helper extraction, naming, duplicated branch cleanup) while preserving behavior.

Keep parameterization strict and ensure all generated SQL remains read-only.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter "RuleSqlCompilerTests|RuleEvaluatorServiceTests"` and then `dotnet test RoslynNavigator.sln`.
  </verify>
  <done>
Code is clearer, tests remain green, and read-only evaluation semantics are unchanged.
  </done>
</task>

</tasks>

<feature>
  <name>Rule SQL compilation and evaluation semantics</name>
  <files>
    src/RoslynNavigator/Services/RuleSqlCompiler.cs
    src/RoslynNavigator/Services/RuleEvaluatorService.cs
    src/RoslynNavigator/Models/RuleViolationModels.cs
    tests/RoslynNavigator.Tests/RuleSqlCompilerTests.cs
    tests/RoslynNavigator.Tests/RuleEvaluatorServiceTests.cs
  </files>
  <behavior>
    Evaluator translates loaded rule predicates into SQL and returns violations from existing snapshot data.

    Cases to lock:
    1) `calls: IRepo.*` -> SQL `LIKE 'IRepo.%'` behavior.
    2) `not:` wraps nested condition via `NOT EXISTS (...)`.
    3) Combined predicates keep correct boolean logic.
    4) Evaluator returns deterministic violation payload for matching entities.
    5) Evaluator uses read-only SQL (`SELECT` only).
  </behavior>
  <implementation>
    Follow Red-Green-Refactor:
    - RED: semantic tests first
    - GREEN: minimal compiler/evaluator implementation
    - REFACTOR: simplify internals with tests as guardrails

    Use SQL parameterization for all dynamic values and wildcard conversions.
  </implementation>
</feature>

<verification>
Run `dotnet test RoslynNavigator.sln --filter "RuleSqlCompilerTests|RuleEvaluatorServiceTests"`.
Run `dotnet test RoslynNavigator.sln`.
</verification>

<success_criteria>
- Wildcard calls predicates are enforced through LIKE semantics
- `not:` blocks are enforced through NOT EXISTS semantics
- Evaluator returns typed violations from snapshot data using read-only queries
</success_criteria>

<output>
After completion, create `.planning/phases/02-rules-engine/02-02-SUMMARY.md`
</output>
