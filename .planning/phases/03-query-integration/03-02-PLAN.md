---
phase: 03-query-integration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/RoslynNavigator/Commands/SnapshotQueryCommand.cs
  - src/RoslynNavigator/Models/SnapshotQueryResults.cs
  - src/RoslynNavigator/Program.cs
  - tests/RoslynNavigator.Tests/SnapshotQueryCommandTests.cs
autonomous: true
requirements: [ASK-01, ASK-02, ASK-03]

must_haves:
  truths:
    - "User can run `roslyn-nav snapshot query --sql \"...\" [--db <path>]` and receive JSON array output"
    - "Output shape remains an array of objects for every successful query"
    - "`snapshot query` never mutates snapshot files and surfaces read-only validation errors"
  artifacts:
    - path: "src/RoslynNavigator/Commands/SnapshotQueryCommand.cs"
      provides: "Query command orchestration for SQL execution against snapshot DB"
      exports: ["SnapshotQueryCommand"]
    - path: "src/RoslynNavigator/Models/SnapshotQueryResults.cs"
      provides: "Stable response contract for query result rows and metadata"
      exports: ["SnapshotQueryResult"]
    - path: "src/RoslynNavigator/Program.cs"
      provides: "CLI wiring for `snapshot query` with `--sql` and optional `--db`"
      contains: "new Command(\"query\""
    - path: "tests/RoslynNavigator.Tests/SnapshotQueryCommandTests.cs"
      provides: "End-to-end command tests for output consistency and read-only enforcement"
      contains: "snapshot query"
  key_links:
    - from: "src/RoslynNavigator/Program.cs"
      to: "src/RoslynNavigator/Commands/SnapshotQueryCommand.cs"
      via: "CLI handler invocation for `snapshot query`"
      pattern: "SnapshotQueryCommand"
    - from: "src/RoslynNavigator/Commands/SnapshotQueryCommand.cs"
      to: "src/RoslynNavigator/Services/SqlReadOnlyGuard.cs"
      via: "Validate SQL before execution"
      pattern: "SqlReadOnlyGuard"
---

<objective>
Ship the user-facing SQL query surface for snapshots with a stable JSON contract and enforced immutability.

Purpose: ASK-01 and ASK-02 deliver Phase 3 user value; ASK-03 must remain enforced for arbitrary SQL input.
Output: `snapshot query` command, query result models, CLI registration, and command-level tests.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-snapshot-foundation/01-03-SUMMARY.md
@.planning/phases/02-rules-engine/02-03-SUMMARY.md
@.planning/phases/03-query-integration/03-01-SUMMARY.md
@src/RoslynNavigator/Program.cs
@src/RoslynNavigator/Commands/SnapshotCommand.cs
@src/RoslynNavigator/Commands/CheckCommand.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement snapshot query command orchestration and stable result contract</name>
  <files>
src/RoslynNavigator/Commands/SnapshotQueryCommand.cs
src/RoslynNavigator/Models/SnapshotQueryResults.cs
  </files>
  <action>
Create `SnapshotQueryCommand` that accepts SQL text and db path, validates SQL through the shared read-only guard from plan 03-01, executes the query, and returns a JSON-serializable result where `rows` is always `List<Dictionary<string, object?>>`.

Ensure column values preserve primitive types where possible (string/number/bool/null) and avoid command-specific schema assumptions so arbitrary SELECT projections are supported.

Return clear failure payloads for invalid SQL, missing DB, and read-only validation failures while preserving existing command JSON error style.
  </action>
  <verify>
Run `dotnet build RoslynNavigator.sln` after adding command and models.
  </verify>
  <done>
`SnapshotQueryCommand` can execute safe SQL and always produce an array-of-objects response contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `snapshot query` CLI surface in Program.cs</name>
  <files>src/RoslynNavigator/Program.cs</files>
  <action>
Refactor `snapshot` into a command group that keeps existing `snapshot --solution ...` behavior and adds a `query` subcommand:
- `snapshot query --sql "..."` (required)
- `snapshot query --db <path>` (optional)

If `--db` is omitted, resolve to the default snapshot path convention used by Phase 1 (`.roslyn-nav/snapshots/<solution>.snapshot.db`) using existing services/patterns; when no deterministic default is possible, return a clear actionable error.

Keep serialization with shared `jsonOptions` and route failures through `OutputError("snapshot_query_error", ...)`.
  </action>
  <verify>
Run `dotnet run --project src/RoslynNavigator -- snapshot query --help` and confirm `--sql` and optional `--db` are documented.
  </verify>
  <done>
CLI exposes `snapshot query` without regressing existing `snapshot` generation usage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add command tests for query output consistency and immutability</name>
  <files>tests/RoslynNavigator.Tests/SnapshotQueryCommandTests.cs</files>
  <action>
Add tests that cover:
1) successful arbitrary SELECT returning JSON array,
2) consistent array-of-object output for different column sets,
3) rejection of mutating SQL (`UPDATE`, `DELETE`, `PRAGMA`, chained statements),
4) optional `--db` behavior (explicit path and default resolution/error),
5) invalid SQL surfaces structured error response.

Use temporary snapshot databases populated with deterministic fixture rows.
  </action>
  <verify>
Run `dotnet test RoslynNavigator.sln --filter SnapshotQueryCommandTests` then `dotnet test RoslynNavigator.sln`.
  </verify>
  <done>
Tests prove ASK-01/ASK-02 contract and ensure ASK-03 immutability for `snapshot query`.
  </done>
</task>

</tasks>

<verification>
Run `dotnet test RoslynNavigator.sln --filter SnapshotQueryCommandTests`.
Run `dotnet run --project src/RoslynNavigator -- snapshot query --sql "SELECT * FROM classes LIMIT 1" --db <temp.snapshot.db>` and validate JSON array output.
</verification>

<success_criteria>
- `snapshot query` executes arbitrary read-only SQL and returns JSON array rows
- Output contract stays stable across different SELECT projections
- Mutating SQL attempts are blocked and snapshot DB remains unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-query-integration/03-02-SUMMARY.md`
</output>
