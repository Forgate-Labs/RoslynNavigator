---
phase: 04-integration-polish
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/RoslynNavigator/Program.cs
  - src/RoslynNavigator/Commands/SnapshotCommand.cs
  - src/RoslynNavigator/Commands/SnapshotQueryCommand.cs
  - src/RoslynNavigator/Commands/CheckCommand.cs
  - tests/RoslynNavigator.Tests/SnapshotCommandTests.cs
  - tests/RoslynNavigator.Tests/SnapshotQueryCommandTests.cs
  - tests/RoslynNavigator.Tests/CheckCommandTests.cs
  - tests/RoslynNavigator.Tests/ProgramHelpTests.cs
autonomous: true
requirements: [INT-02, INT-03]
must_haves:
  truths:
    - "`roslyn-nav --help` still lists existing command groups and includes snapshot/check/query entries"
    - "`snapshot`, `check`, and `snapshot query` continue returning JSON contracts expected by existing tests"
    - "Legacy navigation and write/mutation commands continue to execute without contract regressions"
  artifacts:
    - path: "src/RoslynNavigator/Program.cs"
      provides: "Root command registration including snapshot/check groups after project split"
      contains: "rootCommand.AddCommand(snapshotCommand)"
    - path: "tests/RoslynNavigator.Tests/ProgramHelpTests.cs"
      provides: "Help-surface assertions for command discoverability"
      contains: "--help"
    - path: "tests/RoslynNavigator.Tests/CheckCommandTests.cs"
      provides: "JSON behavior regression coverage for check command"
      contains: "success"
  key_links:
    - from: "src/RoslynNavigator/Program.cs"
      to: "RoslynNavigator.Commands.SnapshotCommand"
      via: "command handler instantiation"
      pattern: "new SnapshotCommand\(\)"
    - from: "src/RoslynNavigator/Program.cs"
      to: "RoslynNavigator.Commands.CheckCommand"
      via: "command handler instantiation"
      pattern: "new CheckCommand\(\)"
    - from: "tests/RoslynNavigator.Tests/*CommandTests.cs"
      to: "CLI executable entrypoint"
      via: "command invocation assertions"
      pattern: "roslyn-nav"
---

<objective>
Polish and lock integration quality by keeping CLI discoverability and output contracts stable after the project split, while proving no regressions across existing command surface.

Purpose: Satisfy INT-02 and INT-03 by verifying help/output compatibility and protecting all existing command contracts.
Output: Updated command wiring/tests with explicit compatibility checks and green full-suite verification.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-integration-polish/04-01-SUMMARY.md
@src/RoslynNavigator/Program.cs
@tests/RoslynNavigator.Tests/SnapshotCommandTests.cs
@tests/RoslynNavigator.Tests/SnapshotQueryCommandTests.cs
@tests/RoslynNavigator.Tests/CheckCommandTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire CLI command handlers to consume split projects without surface changes</name>
  <files>src/RoslynNavigator/Program.cs, src/RoslynNavigator/Commands/SnapshotCommand.cs, src/RoslynNavigator/Commands/SnapshotQueryCommand.cs, src/RoslynNavigator/Commands/CheckCommand.cs</files>
  <action>Update imports/usings and any constructor wiring required after moving snapshot/rules internals into dedicated projects, but preserve exact command names, option names, and JSON error envelope shape already established in prior phases. Keep backward-compatible behavior for `snapshot generate`, `snapshot query`, and `check`.</action>
  <verify>Run `dotnet build RoslynNavigator.sln` and execute `dotnet run --project src/RoslynNavigator -- --help`.</verify>
  <done>CLI compiles and help output still exposes `snapshot`, `snapshot query`, and `check` alongside existing command groups.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit help and JSON contract regression tests for integration commands</name>
  <files>tests/RoslynNavigator.Tests/ProgramHelpTests.cs, tests/RoslynNavigator.Tests/SnapshotCommandTests.cs, tests/RoslynNavigator.Tests/SnapshotQueryCommandTests.cs, tests/RoslynNavigator.Tests/CheckCommandTests.cs</files>
  <action>Add/expand tests that assert command visibility in `--help`, stable option names (`--solution`, `--db`, `--sql`, `--severity`, `--ruleId`), and canonical JSON keys (`success`, `error`, rows/violations payload fields). Reuse existing fixture/setup patterns from prior command tests; avoid introducing brittle whitespace assertions.</action>
  <verify>Run `dotnet test RoslynNavigator.sln --filter "ProgramHelpTests|SnapshotCommandTests|SnapshotQueryCommandTests|CheckCommandTests"`.</verify>
  <done>Integration-command tests prove discoverability and output contracts are unchanged after the project split.</done>
</task>

<task type="auto">
  <name>Task 3: Execute full backward-compatibility regression for existing command surface</name>
  <files>tests/RoslynNavigator.Tests/RoslynNavigator.Tests.csproj</files>
  <action>Ensure the full existing test suite runs after integration, including navigation and staged-edit/dotnet mutation command tests. If tests rely on moved internals, fix only reference/wiring points without changing public command contracts or error-code conventions.</action>
  <verify>Run `dotnet test RoslynNavigator.sln`.</verify>
  <done>All existing tests pass with no regressions, demonstrating compatibility for legacy commands plus new snapshot/rules/query integrations.</done>
</task>

</tasks>

<verification>
Validate `roslyn-nav --help` and `dotnet test RoslynNavigator.sln` both pass after integration wiring.
</verification>

<success_criteria>
- Help output includes `snapshot`, `check`, and `snapshot query` with expected options.
- JSON outputs for snapshot/check/query remain compatible with existing command test expectations.
- Full test suite passes, indicating existing navigation and write/mutation commands were not broken.
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-polish/04-02-SUMMARY.md`
</output>
