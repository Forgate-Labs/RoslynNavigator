---
phase: 04-integration-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - RoslynNavigator.sln
  - src/RoslynNavigator/RoslynNavigator.csproj
  - src/RoslynNavigator.Snapshot/RoslynNavigator.Snapshot.csproj
  - src/RoslynNavigator.Rules/RoslynNavigator.Rules.csproj
  - src/RoslynNavigator.Snapshot/Resources/SnapshotSchema.sql
  - src/RoslynNavigator.Rules/Resources/Rules/architecture.yaml
  - tests/RoslynNavigator.Tests/RoslynNavigator.Tests.csproj
autonomous: true
requirements: [INT-01]
must_haves:
  truths:
    - "Solution includes dedicated Snapshot and Rules class library projects"
    - "CLI project references both new libraries and compiles"
    - "Tests can access Snapshot and Rules types through project references"
  artifacts:
    - path: "src/RoslynNavigator.Snapshot/RoslynNavigator.Snapshot.csproj"
      provides: "Snapshot library assembly with schema resource and snapshot services/models"
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "src/RoslynNavigator.Rules/RoslynNavigator.Rules.csproj"
      provides: "Rules library assembly with YAML resources and rule services/models"
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "RoslynNavigator.sln"
      provides: "Solution-level registration for both new projects"
      contains: "RoslynNavigator.Snapshot"
  key_links:
    - from: "src/RoslynNavigator/RoslynNavigator.csproj"
      to: "src/RoslynNavigator.Snapshot/RoslynNavigator.Snapshot.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"..\\RoslynNavigator.Snapshot\\RoslynNavigator.Snapshot.csproj\""
    - from: "src/RoslynNavigator/RoslynNavigator.csproj"
      to: "src/RoslynNavigator.Rules/RoslynNavigator.Rules.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"..\\RoslynNavigator.Rules\\RoslynNavigator.Rules.csproj\""
    - from: "tests/RoslynNavigator.Tests/RoslynNavigator.Tests.csproj"
      to: "RoslynNavigator.Snapshot and RoslynNavigator.Rules"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"..\\..\\src\\RoslynNavigator\\RoslynNavigator.csproj\""
---

<objective>
Create the physical multi-project structure required for milestone integration by extracting Snapshot and Rules code into dedicated class libraries and wiring project references.

Purpose: Satisfy INT-01 so the CLI can evolve without keeping all snapshot/rules internals in a single executable project.
Output: New `RoslynNavigator.Snapshot` and `RoslynNavigator.Rules` projects added to solution with correct references/resources.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-snapshot-foundation/01-03-SUMMARY.md
@.planning/phases/02-rules-engine/02-03-SUMMARY.md
@.planning/phases/03-query-integration/03-02-SUMMARY.md
@src/RoslynNavigator/RoslynNavigator.csproj
@RoslynNavigator.sln
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `RoslynNavigator.Snapshot` project and move snapshot assets</name>
  <files>src/RoslynNavigator.Snapshot/RoslynNavigator.Snapshot.csproj, src/RoslynNavigator.Snapshot/Services/SnapshotSchemaService.cs, src/RoslynNavigator.Snapshot/Services/SnapshotPathService.cs, src/RoslynNavigator.Snapshot/Services/SnapshotSignalAnalyzer.cs, src/RoslynNavigator.Snapshot/Services/SnapshotExtractorService.cs, src/RoslynNavigator.Snapshot/Models/SnapshotModels.cs, src/RoslynNavigator.Snapshot/Resources/SnapshotSchema.sql</files>
  <action>Create a new class library targeting `net10.0`, move snapshot models/services/schema resource from the CLI project into it, and keep namespaces compatible with current command code to minimize downstream breakage. Preserve embedded resource wiring for `SnapshotSchema.sql` and keep behavior identical to Phase 1/3 summaries (no contract changes or new CLI flags).</action>
  <verify>Run `dotnet build RoslynNavigator.sln` and confirm snapshot-related compile errors are resolved via the new project.</verify>
  <done>`RoslynNavigator.Snapshot` builds successfully and contains all snapshot extraction/schema/path functionality with embedded schema resource intact.</done>
</task>

<task type="auto">
  <name>Task 2: Create `RoslynNavigator.Rules` project and move rules/query-safety assets</name>
  <files>src/RoslynNavigator.Rules/RoslynNavigator.Rules.csproj, src/RoslynNavigator.Rules/Services/RuleLoaderService.cs, src/RoslynNavigator.Rules/Services/RuleSqlCompiler.cs, src/RoslynNavigator.Rules/Services/RuleEvaluatorService.cs, src/RoslynNavigator.Rules/Services/SqlReadOnlyGuard.cs, src/RoslynNavigator.Rules/Models/RuleModels.cs, src/RoslynNavigator.Rules/Models/RuleViolationModels.cs, src/RoslynNavigator.Rules/Resources/Rules/architecture.yaml, src/RoslynNavigator.Rules/Resources/Rules/code-quality.yaml, src/RoslynNavigator.Rules/Resources/Rules/security.yaml</files>
  <action>Create a second class library targeting `net10.0`, move rules engine and SQL guard code plus embedded YAML rule packs into it, and keep runtime behavior and JSON semantics unchanged from Phase 2/3. Ensure the new Rules project references Snapshot project only if required by compile-time dependencies; avoid introducing cyclic references.</action>
  <verify>Run `dotnet test RoslynNavigator.sln --filter "RuleLoaderServiceTests|RuleSqlCompilerTests|RuleEvaluatorServiceTests|SqlReadOnlyGuardTests"`.</verify>
  <done>Rules and SQL read-only guard types compile from `RoslynNavigator.Rules`, embedded YAML packs load, and the focused rule/safety tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Wire solution and project references for split architecture</name>
  <files>RoslynNavigator.sln, src/RoslynNavigator/RoslynNavigator.csproj, tests/RoslynNavigator.Tests/RoslynNavigator.Tests.csproj</files>
  <action>Add both new projects to the solution, remove moved source/resource includes from CLI csproj, and add explicit ProjectReference links from CLI to Snapshot/Rules. Keep tests referencing the CLI project as primary contract, adding direct references only if needed for test compilation. Do not change command names or option signatures in this task.</action>
  <verify>Run `dotnet sln RoslynNavigator.sln list` and `dotnet build RoslynNavigator.sln`.</verify>
  <done>Solution lists four projects (CLI, Snapshot, Rules, Tests), CLI compiles through project references, and build succeeds.</done>
</task>

</tasks>

<verification>
Run `dotnet build RoslynNavigator.sln` and confirm no missing type/resource errors after project extraction.
</verification>

<success_criteria>
- `RoslynNavigator.Snapshot` and `RoslynNavigator.Rules` exist as solution projects.
- `RoslynNavigator` project compiles using ProjectReference dependencies instead of local snapshot/rules source files.
- Existing snapshot/rules functionality remains build- and test-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-polish/04-01-SUMMARY.md`
</output>
